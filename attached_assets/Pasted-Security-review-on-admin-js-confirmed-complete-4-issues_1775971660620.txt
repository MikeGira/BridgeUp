Security review on admin.js confirmed complete — 4 issues fixed, 8 checks clean, server running with exactly 2 pending routes. Go ahead with file 15 — artifacts/bridgeup/server/routes/reports.js. Build all 7 report types as defined in the spec. Each report must pull real live data from Firestore — never placeholder or mock data.
The 7 reports are:
Report 1 — needs_impact: total needs by category, location, urgency, resolution status, resolution rate trend over time, average response time trend, unmet needs analysis showing what needs go unmatched most frequently and where, AI narrative summary from generateReportSummary().
Report 2 — helper_performance: all helpers with metrics — total accepted, resolved, resolution rate percentage, average rating, average response time, last active date — filterable by country, city, helpType category, rating range, and date range — AI insights identifying top performers and patterns.
Report 3 — geographic_coverage: need density vs helper density by region using aggregated location data, coverage gap analysis highlighting areas with high need volume and low helper availability, AI expansion recommendations.
Report 4 — donor_ngo_impact: external-facing grant-ready format, total people helped, needs resolved by category, geographic reach summary, average response time, anonymized impact narratives where permission given, AI narrative — one-click PDF export formatted for funder submission.
Report 5 — financial_grant: grant-formatted impact summary with all metrics, cost per person helped calculation based on operational data, projected reach and growth metrics, formatted for grant committee submission.
Report 6 — compliance: GDPR and CCPA audit showing data held per user type, retention periods, deletion requests completed, helper verification status showing verified vs pending vs rejected vs expired, flagged account log with timestamps and resolution, all compliance data timestamped and retained minimum 7 years.
Report 7 — sms_feature_phone: total inbound SMS needs by date and region, SMS resolution rate compared to app resolution rate, most common need types via SMS, feature phone user geographic distribution.
All reports require: GET /api/reports/:type endpoint with date range query parameter supporting values today, last7days, last30days, last90days, lastyear, and custom with startDate and endDate. POST /api/reports/:type/export/pdf that generates a branded PDF using pdfkit with tenant logo placeholder, report title, date range, generation timestamp, page numbers, and all data formatted in tables. POST /api/reports/:type/export/excel that generates an Excel file using xlsx with proper column headers, formatted data rows, and a summary sheet. generateReportSummary() from claude.js called for AI narrative on every report. All queries tenant-scoped — admin sees their tenant data only, superadmin sees aggregate across all tenants. Rate limiter at 10 requests per minute per authenticated user on all report generation and export endpoints. Phone numbers redacted to last 4 digits in all report data. Scheduled automated delivery via the scheduler service: weekly every Monday at 8am, monthly on the 1st, quarterly, and annual — using node-cron in server/services/scheduler.js