reports.js security review complete — 4 issues fixed, 8 checks clean, server running with exactly 1 pending route. Go ahead with file 16 and final server file — artifacts/bridgeup/server/routes/reviews.js. Build it with these specific capabilities:
POST /api/reviews/submit — requireAuth — accepts matchId, rating integer 1 to 5, comment optional maximum 500 characters, reviewType which must be either need_to_helper or helper_to_need — validates match exists in Firestore and has status resolved — checks reviewer has not already submitted a review for this match using a duplicate guard — stores review document in reviews Firestore collection — updates helper rating field using running average calculation: newRating = ((currentRating times totalRatings) plus newRating) divided by (totalRatings plus 1) — increments totalRatings by 1 — checks if helper has 3 or more reviews with rating 1 or 2 in the last 30 days and if so automatically sets helper status to suspended and writes an admin notification document — creates an Impact Card record in Firestore impact_cards collection with fields: needCategory, helperName, userCity, resolvedAt, rating, sharePermission defaulting to false — rate limit 10 submissions per hour per user.
GET /api/reviews/my — requireAuth — returns all reviews where reviewerId equals authenticated user, sorted by createdAt descending.
GET /api/reviews/helper/:id — no auth required — validates helperId format — only returns reviews for approved helpers — returns paginated reviews sorted by createdAt descending — never returns reviewer phone numbers.
GET /api/reviews/pending — requireAuth — returns needs that are in resolved status belonging to the authenticated user that have no review submitted yet — these are used to prompt the review flow in the UI.
POST /api/reviews/:id/flag — requireAuth — validates reviewId format — allows a user to flag an inappropriate review — stores flagReason — marks review as isFlagged true — writes admin notification.
GET /api/reviews/flagged — admin and superadmin only — returns all flagged reviews with full context — tenant scoped.
PATCH /api/reviews/:id/moderate — admin only — accepts action of either approve or remove — if remove sets review isVisible to false — writes audit log entry with moderatorId, action, timestamp — writes notification to original reviewer.
All review documents must store these exact fields: matchId, needId, helperId, reviewerId, reviewerRole, rating, comment, reviewType, createdAt, isFlagged defaulting to false, flagReason defaulting to null, isModerated defaulting to false, moderatedAt defaulting to null, moderatedBy defaulting to null, isVisible defaulting to true.
After all code is written restart the server and confirm zero pending routes — all routes loaded silently.