Excellent security review — 4 issues found and fixed, server clean. Go ahead with file 11 — artifacts/bridgeup/server/routes/voice.js. Build it with these specific endpoints and behaviors: POST /voice/answer that verifies the Twilio request signature using validateTwilioSignature as the absolute first operation — rejects with 403 and empty TwiML if invalid — extracts the caller phone number from the From field and derives the country code — calls buildGreetingTwiML() from voice-service.js to generate the language-appropriate greeting — and returns valid TwiML. POST /voice/process that verifies Twilio signature first — extracts CallSid, SpeechResult, and caller phone — validates CallSid matches the Twilio pattern CA followed by 32 lowercase hex characters using regex before using it as a Firestore document path — sanitizes the SpeechResult transcription by stripping HTML tags, null bytes, control characters, and Unicode bidirectional characters and caps at 500 characters — calls processVoiceTurn() from voice-service.js which handles the 5-turn conversation limit and Firestore state persistence — and returns valid TwiML with the AI spoken response. POST /voice/complete that verifies Twilio signature — calls buildGoodbyeTwiML() from voice-service.js — logs the completed call to audit_logs with phone redacted to last 4 digits — and returns valid TwiML with hangup. POST /voice/status that verifies Twilio signature — validates CallStatus against the Twilio call status allowlist queued, ringing, in-progress, canceled, completed, failed, busy, no-answer — logs call duration, status, and outcome to audit_logs with phone redacted — returns 204 with no body. GET /api/voice/calls that returns voice call history for admin and superadmin — but note that voice_messages documents store no tenantId just like sms_queue, so restrict to superadmin only for now with a code comment explaining that admin access requires adding tenantId to processVoiceTurn writes first — include cursor-based pagination, limit parameter, and phone numbers redacted to last 4 digits in all responses. Add a voiceLimiter if not already applied to the voice mount in index.js — 200 requests per minute matching the SMS limiter. Every endpoint that Twilio calls must return valid TwiML even on errors — never plain text. Signature verification must be the absolute first operation.