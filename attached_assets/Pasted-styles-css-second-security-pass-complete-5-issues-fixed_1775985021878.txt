styles.css second security pass complete — 5 issues fixed including GPU animation performance fixes and Safari webkit prefixes. Pushed to GitHub. Go ahead with file 20 — artifacts/bridgeup/public/js/voice.js. Build the complete Web Speech API voice interface with these specific capabilities:
Expose everything on window.VoiceInterface — no import/export syntax. Call VoiceInterface.init() when DOM is ready.
Speech recognition: use window.SpeechRecognition or window.webkitSpeechRecognition with feature detection first. If not supported hide #voice-btn and show #voice-fallback-input text area instead. Set continuous false, interimResults true, maxAlternatives 1. Language from localStorage key bridgeup_language defaulting to browser language.
Voice button states: manage by adding and removing CSS classes state-idle state-listening state-speaking on #voice-btn element. Tapping idle button starts recognition. Recognition auto-ends on speechend. Show interim transcript in #voice-transcript. On final result call window.ChatInterface.submitVoiceMessage(transcript). On error show human-readable message in #voice-error — not-allowed shows Please allow microphone access, no-speech shows I did not hear anything please try again, network shows Voice recognition needs an internet connection.
Text to speech: speakResponse(text, langCode) function cancels current speech, creates SpeechSynthesisUtterance with lang langCode rate 0.9 pitch 1.0, selects best available voice using getVoices() preferring local over remote, calls speechSynthesis.speak(). Adds state-speaking class during speech. On utterance end returns to state-idle. Full try-catch wrapper.
Language support: setLanguage(code) function on VoiceInterface. Codes: en-US, fr-FR, rw-RW falling back to fr-FR for voice selection, sw-KE, es-ES, ar-SA. Store in localStorage bridgeup_language.
Audio chime: 440hz sine wave 150ms using Web Audio API on listen start. No audio file. Try-catch wrapper.
Accessibility: aria-label Tap to speak on voice button. aria-live polite region #voice-status announcing state changes. Text fallback when voice not supported with proper label.
Safari iOS: full try-catch on all Speech API calls. Handle immediate onend with no results by showing please tap and speak again. Check for HTTPS and warn if on HTTP.