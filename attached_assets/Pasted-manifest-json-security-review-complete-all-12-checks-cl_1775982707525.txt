manifest.json security review complete — all 12 checks clean, zero issues. Pushed to GitHub. Go ahead with file 18 — artifacts/bridgeup/public/service-worker.js. Build a complete production-ready service worker with these specific capabilities:
Cache versioning: const CACHE_VERSION = 'v1' and const CACHE_NAME = 'bridgeup-' + CACHE_VERSION at the very top — incrementing CACHE_VERSION forces a full cache refresh on next visit.
Static pre-cache on install event: pre-cache these exact paths — /, /manifest.json, /css/styles.css, /js/app.js, /js/map.js, /js/chat.js, /js/voice.js, /images/icon-192.png, /images/icon-512.png. Use waitUntil with a cache.addAll() call. If any asset fails to cache, log the error but do not fail the install — the app must still work even if pre-caching partially fails.
Fetch event routing with three strategies: Strategy 1 — cache-first for static assets matching extensions .css .js .png .jpg .svg .woff2 .woff or the path /images/ — return from cache immediately, update cache in background. Strategy 2 — CartoDB tile caching for URLs containing basemaps.cartocdn.com — cache-first with a maximum of 500 cached tiles — when the limit is exceeded delete the oldest tile entries. Strategy 3 — network-first for all /api/ requests — try network first, on network failure return cached response if available, if no cache return JSON response with status offline true and message Your request has been saved and will be sent when your connection returns. Never cache POST requests under any strategy. Never cache /api/auth/ routes under any strategy.
Offline submission queue using IndexedDB: when a fetch to POST /api/needs or POST /api/reviews/submit fails because the user is offline, store the full request details in IndexedDB under store name pending_submissions. On the sync event named sync-submissions replay all queued submissions and remove them from IndexedDB on success. Wrap sync registration in if sync in self.registration to handle Safari which does not support background sync.
Push notification handler: add self.addEventListener push that extracts the data payload, calls self.registration.showNotification with title BridgeUp, the message from payload, icon /images/icon-192.png, and badge /images/icon-192.png. Wrap in try catch — if push is not supported fail gracefully.
Activate event: call self.clients.claim() immediately to take control without page reload. Delete all caches whose name does not match CACHE_NAME to clean up old versions.
Safari compatibility: wrap all uses of registration.sync and PushManager in feature detection checks — if sync in self.registration and if PushManager in self — so the service worker never throws on iOS Safari which has partial service worker support.