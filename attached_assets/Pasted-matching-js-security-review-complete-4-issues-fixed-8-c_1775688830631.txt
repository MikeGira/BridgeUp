matching.js security review complete — 4 issues fixed, 8 checks clean, server running with exactly 7 pending routes. Go ahead with file 13 — artifacts/bridgeup/server/routes/payments.js. Build it with these specific capabilities covering all three payment processors:
Stripe endpoints: POST /stripe/webhook that verifies the Stripe webhook signature using stripe.webhooks.constructEvent with STRIPE_WEBHOOK_SECRET from raw request body before processing anything — handles these events: checkout.session.completed to unlock Pro features, invoice.payment_succeeded to confirm renewal, invoice.payment_failed to flag account and send payment failure SMS, customer.subscription.deleted to downgrade tenant to community tier and send cancellation SMS, customer.subscription.updated to update tenant plan in Firestore. POST /api/payments/stripe/create-checkout that creates a Stripe Checkout Session for Pro plan signup — requires auth — returns the checkout URL. POST /api/payments/stripe/portal that creates a Stripe Customer Portal session for subscription management — requires auth. POST /api/payments/stripe/invoice that creates a Stripe Invoice for Enterprise clients — admin only.
Africa's Talking endpoints: POST /africastalking/webhook that verifies the Africa's Talking callback — handles mobile money payment confirmation — updates Firestore subscription on confirmed payment — sends SMS confirmation in Kinyarwanda or Swahili based on country — returns 200. POST /api/payments/mobilemoney/initiate that initiates an MTN or Airtel mobile money payment request — requires auth — validates phone number format — calls Africa's Talking payments API — returns pending status.
Flutterwave endpoints: POST /flutterwave/webhook that verifies Flutterwave signature using FLW_SECRET_KEY — handles payment confirmation — updates Firestore subscription — returns 200.
Shared: GET /api/payments/subscription that returns the current tenant subscription status and plan details — requires auth. The payment router logic must auto-select processor based on user country — Canada and USA use Stripe, Rwanda and East Africa use Africa's Talking, other Africa uses Flutterwave. All webhook handlers must use raw request body for signature verification — not parsed JSON. All amounts must be set server-side — never trust client-submitted prices. Subscription status updates must write to the subscriptions Firestore collection with all fields from the spec schema. Failed payments must send SMS notification to the account admin in their language.